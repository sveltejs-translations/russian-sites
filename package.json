{
  "name": "russian-sites",
  "description": "Russian translation of the svelte sites",
  "scripts": {
    "download": "trpatcher download",
    "apply": "trpatcher apply",
    "update": "node scripts/update",

    "dev:svelte": "node scripts/dev --svelte",
    "build:svelte": "node scripts/build --svelte",
    "start:svelte": "node scripts/start --svelte",

    "dev:kit": "node scripts/dev --kit",
    "build:kit": "node scripts/build --kit",
    "start:kit": "node scripts/start --kit"
  },
  "dependencies": {
    "trpatcher": "^1.0.5"
  },
  "devDependencies": {
    "node-watch": "^0.6.4"
  }
}
